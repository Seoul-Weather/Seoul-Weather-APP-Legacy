import { View, Text } from "react-native";

export const Spot = () => {
    return (
        <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
            <Text>Spot Screen</Text>
        </View>
    );
};
